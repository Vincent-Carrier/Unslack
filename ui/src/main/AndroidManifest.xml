<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="vincentcarrier.todo">

  <application>
    <activity android:name=".screens.tasklist.TasksActivity"/>
    <activity
        android:name=".screens.projectlist.ProjectsActivity"
        android:label="@string/title_activity_project_list"
        android:theme="@style/AppTheme.NoActionBar">
      <intent-filter>
        <action android:name="android.intent.action.MAIN"/>

        <category android:name="android.intent.category.LAUNCHER"/>
      </intent-filter>
    </activity>
    <activity android:name=".screens.redirect.RedirectActivity">
    </activity>
  </application>

</manifest>